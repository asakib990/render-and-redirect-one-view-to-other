<odoo>
    <template id="index">
        <div class="container">
            <form action="/my_module/create_data" method="post">
                <label for="name">Name:</label>
                <input type="text" name="name"/>
                <label for="description">Description:</label>
                <textarea name="description"></textarea>
                <label for="date">Date:</label>
                <input type="date" name="date"/>
                <input type="submit" value="Submit"/>
            </form>
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="data" t-as="d">
                        <tr>
                            <td>
                                <t t-esc="d.name"/>
                            </td>
                            <td>
                                <t t-esc="d.description"/>
                            </td>
                            <td>
                                <t t-esc="d.date"/>
                            </td>
                        </tr>
                    </t>
                </tbody>
            </table>
        </div>
    </template>

    <data>
        <record id="web_view_index" model="ir.ui.view">
            <field name="name">web_view.index</field>
            <field name="model">website.data</field>
            <field name="type">qweb</field>
            <field name="arch" type="xml">
                <template inherit_id="website.layout">
                    <div class="container">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="row">
                            <div class="col-lg-12">
                                <h1>My Module</h1>
                                <t t-call="web_view.index"/>
                            </div>
                        </div>
                    </div>
                </template>
            </field>
        </record>
    </data>

</odoo>


